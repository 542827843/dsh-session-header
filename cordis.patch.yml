# The layer applied when a profile lists this bundle.
# Install: dsh plugin --profile <name> add github:homily707/dsh-session-header
#
# Configuration:
#   header - HTTP header name to inject (default x-session-id)
#   value  - optional fixed value; unset means "the harness session id of the
#            call in flight" (calls without one get no header)
- insert:
    - id: session-header
      name: dsh-session-header
      config:
        header: x-session-id
        # value: my-fixed-session-id
